name: 🛠️ Build NotoSansSuper font

on:
  push:
    branches:
      - NotoSansSuper
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  packages: write
  id-token: write

env:
  FONT_DIR: tools/fonts
  BUILD_DIR: build

jobs:
  build:
    name: 📦 Build ZIP Artifact
    runs-on: ubuntu-latest
    steps:
      - name: 📦 Checkout repository
        uses: actions/checkout@v6

      - name: 📂 Create directories
        run: |
          mkdir -p ${{ env.FONT_DIR }} ${{ env.BUILD_DIR }}

      - name: 🌐 Download fonts from urls.txt
        run: |
          grep -Ev '^\s*(#|$)' tools/fonts/urls.txt | while read -r path; do
            if [[ "$path" =~ ^https?:// ]]; then
              url="$path"
            else
              url="https://${path#//}"
            fi

            file="${url##*/}"
            echo "⬇️ Downloading $url → ${{ env.FONT_DIR }}/$file"
            wget -q --timeout=30 --tries=3 "$url" -O "${{ env.FONT_DIR }}/$file"
          done

      - name: ⚙️ Make all font-tools executable
        run: |
          chmod +x tools/{merge_fonts.sh,otfccdump,merge-otd,otfccbuild}

      - name: 🔗 Merge fonts into NotoSansSuper.otf
        run: |
          cd tools
          ./merge_fonts.sh
          mv NotoSansSuper.otf ../${{ env.BUILD_DIR }}/
          mv notosanssuper.otd ../${{ env.BUILD_DIR }}/

      - name: 📁 Copy fonts directory to build
        run: |
          cp -r ${{ env.FONT_DIR }} ${{ env.BUILD_DIR }}/

      - name: ⬆️ Upload build artifact
        uses: actions/upload-artifact@v7
        with:
          name: font-artifact-NotoSansSuper
          path: ${{ env.BUILD_DIR }}
